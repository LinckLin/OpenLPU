# --- 本地环境与缓存 ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# --- 可再生成的大产物 ---
golden/
rtl/ref/asicsnap/
asic/gen/
asic/dc/db/
asic/dc/gen/
asic/dc/gen_full/
asic/vcs/simv
asic/vcs/csrc*/
asic/vcs/gen/
alib-52/

# --- 工具痕迹与构建产物 ---
abc.history
.history_sta
*.log
*.jou
*.pvl
*.pvk
*.syn
*.mr
*.svf
lc_output.txt

# --- Verilator 构建目录（obj_*/ 前缀与 *_obj/ 后缀均覆盖） ---
obj_*/
*_obj/

# --- VCS/DA 运行时目录（.daidir 内含环境快照，可能携带凭据，务必不上传） ---
*.daidir/

# --- SMIC28 SRAM 宏物理视图（GDS2/CDL 版图，MB 级可再生，GEN.md 有生成命令） ---
asic/sram_macros/**/*.gds2
asic/sram_macros/**/*.cdl
asic/sram_macros/**/*.clf
# --- DC 综合网表（MB 级可再生）；.rpt 时序报告保留 ---
asic/dc/reports/*.v
asic/dc/reports/*.STALE-presto
